- 분기 flow account(영업이익, 당기순이익 등)을 직접 계산해서 넣었는데 LLM이 이 수치가 누적인지 여부를 제대로 판별하지 못해서 prompt를 추가함.

- 역사상 고·저가 돌파를 지표 중 하나로 사용하고자 하였으나 OHLCV를 과거 180일분만 fetch하게끔 세팅되어 있어 최근 6개월 고·저가가 나오는 문제 발생. 별도의 캐시를 빌드하는 것보다는 최근 5년 정도를 잡으면 오히려 간단하며 목적적합할 것 같아 history days를 1825일로 변경함.

- 일부 국내 기업의 손익계산서 항목을 찾아내지 못하는 문제가 발생함. DART API의 제공 라벨과 직접 설정한 계정명의 불일치로 인한 것으로 판단하여 매핑 함수를 추가.

- DART의 연간보고서 제외 분·반기 보고서는 3개월 단위로 값을 fetch한다는 것을 확인하여 기존의 fetch_financial node를 전면 수정. 제대로 의도한 값이 도출됨 확인.

- 로컬(ollama) 모델을 사용하여 성능을 테스트해봄. qwen2.5:7b 시도했는데 답변 품질이 매우 낮아 qwen2.5:14b를 설치하여 재테스트. 이 역시 성능이 그리 좋지 않아보임..

- llm이 갭하락과 갭상승을 제대로 구별하지 못하여 라벨을 up down으로 구분함.